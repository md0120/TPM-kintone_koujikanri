# SMC 工事管理 — プロジェクトメモ

最終更新: 2026-09-20（SMC設備機器台帳がサード社製「管理ロイド」へ移行しkintoneから離れたため、
共通ナレッジファイルを本プロジェクト配下に切り離して自己完結させた）

## 📌 kintone開発 共通ナレッジ（本プロジェクト内で完結）

`docs/kintone開発共通ナレッジ.md` に、特定アプリに限定されない「kintoneプラットフォーム自体の
挙動・罠・設計パターン」をまとめている。旧SMC設備機器台帳プロジェクトと共有していた時期の
記録も含むが、そちらはkintone運用を終了したため、現在はSMC工事管理専用のファイルとして
このプロジェクト内で管理する（`../kintone開発共通ナレッジ.md`は参照しない）。

- **セッション開始時**: 必ず`docs/kintone開発共通ナレッジ.md`にも目を通すこと
- **新しい発見があった時**: このアプリ固有の話ではなく「kintone自体の挙動・罠・汎用パターン」だと判断したら、
  都度そのファイルに追記すること（個別アプリのフィールド名・業務ロジックは`docs/`配下の作業ログ・
  フィールド対応表等、本プロジェクト側の記録に書く）

## プロジェクト構成（参考）

- `app34/` `app37/` `app38/` `app40/` `app43/` `app45/` `app54/` `app55/` — 各kintoneアプリのカスタマイズ・関連ファイル
- `shared/` — 複数アプリで共用するカスタムJS（共通ナビゲーションバー等）
- `docs/` — 作業ログ
- `data/` `mockups/` `lineworks-gas/` — その他データ・モックアップ・LINE WORKS連携用GASスクリプト

本ファイルは2026-08-02にkintone開発共通ナレッジの参照のために新規作成したもの。プロジェクト固有の
詳細な仕様は`docs/`配下の作業ログを参照。
