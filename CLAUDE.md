# SMC 工事管理 — プロジェクトメモ

最終更新: 2026-08-02（kintone開発共通ナレッジの参照を新設）

## 📌 kintone開発 共通ナレッジ（全プロジェクト横断）

`../kintone開発共通ナレッジ.md`（本プロジェクトの一つ上の階層）に、SMC設備機器台帳・SMC工事管理など
kintoneを使う全プロジェクトで共有する「特定アプリに限定されない、kintoneプラットフォーム自体の
挙動・罠・設計パターン」をまとめている。

- **セッション開始時**: 必ず`../kintone開発共通ナレッジ.md`にも目を通すこと
- **新しい発見があった時**: このアプリ固有の話ではなく「kintone自体の挙動・罠・汎用パターン」だと判断したら、
  都度そのファイルに追記すること（個別アプリのフィールド名・業務ロジックは`docs/`配下の作業ログ等
  本プロジェクト側の記録に書き、共通ナレッジファイルには書かない）
- リアルタイム同期ではない点に注意（他プロジェクトのセッションが次に開かれた時に反映される）

## プロジェクト構成（参考）

- `app34/` `app37/` `app38/` `app40/` `app43/` `app45/` `app54/` `app55/` — 各kintoneアプリのカスタマイズ・関連ファイル
- `shared/` — 複数アプリで共用するカスタムJS（共通ナビゲーションバー等）
- `docs/` — 作業ログ
- `data/` `mockups/` `lineworks-gas/` — その他データ・モックアップ・LINE WORKS連携用GASスクリプト

本ファイルは2026-08-02にkintone開発共通ナレッジの参照のために新規作成したもの。プロジェクト固有の
詳細な仕様は`docs/`配下の作業ログを参照。
